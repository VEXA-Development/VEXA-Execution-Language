return {
  -- Whether to verify functions exist during compilation
  CompilerFunctionCheck = true,
  
  -- Enable debug printing in compiler and executor
  DebugMode = false,
  
  -- Maximum instruction limit to prevent infinite loops (future feature)
  MaxInstructions = 1000,
}
