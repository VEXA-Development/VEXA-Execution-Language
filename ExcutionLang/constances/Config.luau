return {
  CompilerFunctionCheck = false,  -- checks if function passed is valid function
  CompilerCacheCheck = true,      -- checks if value being read is in cache. WARNING might be slow for large codes.


  OnErrorStop = true,             -- if error while excuting program stops
}